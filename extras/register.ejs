<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Document</title>
	</head>
	<body>

		
		<h1>Registration</h1>
		<form id="reg-form" method="post" action="/users/register">
			<div>
				<label>Name</label>
				<input type="text" autocomplete="on" name="name" placeholder="" />
			</div>
			<div>
				<label>email</label>
				<input type="email" autocomplete="off" name="email" placeholder="" />
			</div>
			<div>
				<label>UserName</label>
				<input type="text" autocomplete="off" name="username" placeholder="" />
			</div>
			<div>
				<label>password</label>
				<input type="password" autocomplete="off" name="password" placeholder="" />
			</div>
			<!-- <div>
				<label>Confirm password</label>
				<input type="password" autocomplete="off" name="password2" placeholder="" />
			</div> -->

			<input type="submit" value="Submit Form" />
		</form>

		<!-- <script>
			const form = document.getElementById('reg-form')
			form.addEventListener('submit', registerUser)

			async function registerUser(event) {
				event.preventDefault()
				const name = document.getElementById('name').value
				const username = document.getElementById('username').value
				const password = document.getElementById('password').value

				const result = await fetch('/api/register', {
					method: 'POST',
					headers: {
						'Content-Type': 'application/json'
					},
					body: JSON.stringify({
						name,
						username,
						password
					})
				}).then((res) => res.json())

				if (result.status === 'ok') {
					// everythign went fine
					alert('Success')
				} else {
					alert(result.error)
				}
			}
		</script> -->
	</body>
</html>
